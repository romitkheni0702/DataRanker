# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (client/ and server/)
node_modules/
/.pnp
.pnp.js

# testing
coverage/

# production builds
build/
/client/build

# env / secrets — never commit. .env.example IS committed.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Python bytecode
__pycache__/
*.pyc

# Trade secret — never commit or push
LOGIC.md

# Nested Next.js + Express rewrite (its own git repo, kept as reference)
/dataranker
